# ANOVA Webアプリ 作業記録

## 確定事項

- 2026年9月17日、ANOVA君4.5.1相当の埋め込みコードを公式版4.8.9へ更新した。
- 公式配布ファイルは改変せず `anovakun_489.R` として保存し、`server.R` から読み込む。
- `anovakun_489.R` は `.gitattributes` で改行コードの自動変換を止め、公式配布ファイルのバイト列とSHA-256を保持する。
- 入力形式と分析オプション（Mauchly、auto、Holm）は変更しない。
- 効果量は利用者に選択させず、偏イータ二乗と一般化イータ二乗を常に併記する。
- 効果量の95%信頼区間は通常分析と分離し、ボタンを押した場合だけ2,000回のブートストラップで計算する。
- 入力データは分析前に列数、欠損値、数値列、条件数、列見出しを検査し、該当する行・列を含むエラーを画面に表示する。
- 分析方法を変更したときは、対応する内蔵サンプルと水準数を自動入力する。利用者が貼り付けたデータは分析方法を変更した場合に置き換わる。
- 分析結果はテキストで保存できるようにし、生成日時、R、ANOVA君、計画型の情報を先頭に記録する。効果量CIを計算済みの場合は同じファイルに含める。
- ANOVA表の通常表示では、A・B・sを実際の要因名と `Participant` を使った表示へ変換する。ANOVA君の原出力は折りたたみ式のTechnical outputとして残す。
- 被験者間要因名と従属変数名はデータの列見出しを使用する。ワイド形式ではデータから判定できない被験者内要因名と従属変数名だけを入力欄で指定する。
- グラフは個別観測値を既定で表示する。被験者間要因では半透明のjitter点、被験者内要因では参加者別の細い半透明線を表示し、平均と95% CIを重ねる。
- ANOVA君の案内先は `https://riseki.cloudfree.jp/?ANOVA%E5%90%9B` とする。
- Showcaseのコードは画面幅にかかわらずアプリの下に表示し、右側へ切り替えるリンクは表示しない。
- Design欄は内部値を変えず、説明付きの縦並びラジオカードで表示する。
- Plot節の直前に区切り線を表示し、2要因分析で2枚のグラフを表示する場合は間を32px空ける。
- グラフの日本語にはmacOSで `Hiragino Sans`、本番Linuxで `Noto Sans CJK JP` を使用する。

## 重要な判断と理由

- ANOVA君の関数群をShinyのreactive内に埋め込む構造を廃止した。入力更新ごとの関数再定義を避け、公式コードとの差分を追跡しやすくするため。
- 入力データを共有reactiveで1回だけ読み込み、250ミリ秒のdebounceを設定した。入力途中の不要な再計算を減らすため。
- `options(warn=-1)` を廃止した。互換性や数値計算の警告を隠さないため。
- UIから参照されていない未定義の `check()` 出力を削除した。
- ブートストラップを通常分析から分離した。初期データで通常分析は約1.5秒、2,000回のブートストラップは約12秒を要したため、入力更新ごとの自動実行はサーバー負荷が大きい。
- CI計算後にデータまたは分析条件が変わった場合は、古いCIを消して再計算を案内する。変更前の結果を誤って使用することを防ぐため。
- 入力検査を通常分析、グラフ、効果量CIの共通入口に置いた。分析経路によって検査内容が変わることを防ぐため。
- サンプルは `Input Examples` タブに既にある5種類を再利用し、同じデータをコード内で二重管理しない。
- ANOVA君の読込を `sys.source(..., encoding=)` から `source(..., local=, encoding=)` に修正した。`sys.source` は `encoding` 引数を受け付けず、アプリ起動時にエラーになるため。
- ANOVA君へ水準数だけでなく、要因名を名前、実際の水準名を値とする引数を渡す。公式コードを変更せず、分散分析表、事後分析、効果量CIへ同じ名称を反映できるため。
- 反復測定要因を横軸にした図のCIは、参加者内で正規化しMorey補正を加えたwithin-subject 95% CIとした。被験者間要因を横軸にした図は通常のt分布による95% CIとした。
- 旧グラフで使用していた `sciplot` の読込を廃止した。新しいグラフは `ggplot2` に統一し、環境依存と重複処理を減らした。
- 旧グラフ関数760行を削除し、5計画共通の描画関数へ集約した。軸範囲、個別データ、平均、CI、要因名の処理を1か所で管理する。
- Shiny Showcaseは本文幅が1350pxを超えるとコードを右側へ自動移動する。`body` の最大幅を1350pxに固定して自動移動を発生させず、位置切替リンクもCSSで非表示にした。
- Designの値は分析処理、入力検査、サンプル自動入力が参照しているため変更せず、利用者に見える名称と説明だけを置き換えた。
- Design切替時は、250ミリ秒のdebounce後に検査済みデータとその検査に使ったDesignを一組で分析へ渡す。切替直後に旧データと新Designが一時的に混在することを防ぐため。
- 日本語文字化けはデータの文字コードではなく、本番サーバーに日本語フォントがなく、RのPNG描画が代替フォントを使えなかったことが原因だった。追加Rパッケージは使わず、アプリ専用の `fontconfig.conf` で共通フォント領域を参照する。

## 事実確認

- 確認日: 2026年9月17日。
- 公式配布元: `https://riseki.cloudfree.jp/?ANOVA%E5%90%9B`
- 公式ファイル: anovakun 4.8.9（2023年5月1日公開）。
- SHA-256: `c668efb4445ce9d27b2c7433e93338850fdd623c06a771b4a54a7db71c145f67`
- 公開中のR 4.5.2環境では、内蔵の2×3被験者内例の誤差平方和がANOVA君4.8.9およびR標準の反復測定ANOVAと一致しなかった。
- 更新後の同例は `s x A = 973.4166667`、`s x B = 2387.3666667`、`s x A x B = 1213.4333333`。交互作用は `F(2, 18) = 8.6054171, p = 0.0023843`。
- 新グラフへの移行前の検証では、ローカルRに `sciplot` がないため描画依存を除いたShinyモックセッションを使用した。入力検査後の分析結果生成とテキスト保存、保存ファイル内のR 4.5.2、ANOVA君4.8.9、計画型、一般化イータ二乗を機械検査した。
- 新グラフへの移行後は `sciplot` が不要になった。2026年9月17日にローカルShinyサーバーを起動し、初期HTMLが正常に返ることを確認した。5計画すべてについてShinyモックセッションで結果表とPNGグラフを生成し、混合計画の2枚は画像として目視確認した。
- 2026年9月17日、GitHubの `master` と `https://langtest.jp/shiny/anova/` をコミット `fb321a0` へ更新した。本番R 4.5.2で4本の回帰テストがすべて成功し、公式ファイルのSHA-256も上記の値と一致した。
- 公開画面で初期データの入力検査、要因名を使ったANOVA表、偏イータ二乗・一般化イータ二乗、2枚のグラフを確認した。効果量95% CIボタンも2,000回のブートストラップを完了し、サーバー上の回帰テストでは12.46秒だった。
- 2026年9月17日、Showcaseコード位置の修正を本番へ反映した。幅1,409pxのChromeで本文幅が1,350pxとなり、コード領域がアプリ右側ではなく下側にあり、位置切替リンクが非表示であることを確認した。
- Designカードへの変更後、ローカルShiny画面で `Between × Between` を選ぶと180行3列の対応サンプル、`ABs`、`Classroom` の結果表へ更新されることを確認した。切替時の警告はなく、Plot見出し上の区切り線と2グラフ間32pxもDOMで確認した。
- 2026年9月17日、DesignカードとPlotレイアウトをコミット `60c7e7b` で本番へ反映した。公開画面でも初期選択、区切り線、32pxのグラフ間隔、および `Between × Between` への切替後のサンプルと結果表を確認した。
- 本番Ubuntu 22.04には日本語フォントがなく、`systemfonts`、`ragg`、`showtext` も未導入だった。Ubuntu公式の `fonts-noto-cjk` 1:20220127+repack1-1からRegularとBoldを `/srv/shiny-server/.fonts/` へ配置した（合計38MB）。
- 本番R 4.5.2でNoto Sans CJK JPを指定した試験PNGを生成し、日本語のタイトル、横軸、縦軸を目視確認した。ローカルShinyではHiragino Sansで `得点 by 測定時点` と両軸の日本語が正常に表示され、描画警告がないことを確認した。

## ファイル構成

- `server.R`: Shinyサーバー処理。
- `validation.R`: 計画型の定義、入力検査、サンプル対応表。
- `presentation.R`: 要因名・水準名の導出、ロング形式変換、グラフ用CI、読みやすいANOVA表の生成。
- `fontconfig.conf`: 本番Shinyプロセスが共通領域のNoto CJKフォントを参照するための設定。
- `anovakun_489.R`: 公式ANOVA君4.8.9の未改変コピー。
- `tests/regression_anova.R`: 内蔵5例と2要因被験者内の基準値を検証するテスト。
- `tests/regression_effect_size_ci.R`: 偏イータ二乗と一般化イータ二乗のブートストラップ95% CIを検証するテスト。
- `tests/regression_validation.R`: 5計画の入力検査とサンプル対応を検証するテスト。
- `tests/regression_presentation.R`: 5計画の要因名、結果表、グラフ用データとCIを検証するテスト。
- `done/2026-09-17_before_anovakun489/`: 更新前のコード一式。
- `done/2026-09-17_before_effect_size_ci/`: 効果量CI機能を追加する直前のコード一式。
- `done/2026-09-17_before_validation_download_samples/`: 入力検査、結果保存、サンプル自動入力を追加する直前のコード一式。
- `done/2026-09-17_before_readable_output_plots/`: 読みやすい結果表と新しいグラフを追加する直前のコード一式。
- `done/2026-09-17_before_showcase_bottom/`: Showcaseのコード位置を下へ固定する直前の `ui.R` と作業記録。
- `done/2026-09-17_before_design_cards_layout/`: Designカード、区切り線、グラフ間隔を変更する直前の `ui.R` と作業記録。
- `done/2026-09-17_before_japanese_plot_font/`: 日本語グラフ対応を追加する直前の `server.R` と作業記録。
- `done/` と `.DS_Store` は `.gitignore` でGit管理対象外とする。旧版はローカルに保持し、本番・GitHubへは配備しない。

## 再現方法

アプリのフォルダで次を実行する。

```sh
Rscript tests/regression_anova.R
Rscript tests/regression_effect_size_ci.R
Rscript tests/regression_validation.R
Rscript tests/regression_presentation.R
```

成功時は次を表示する。内蔵5例の偏イータ二乗と一般化イータ二乗の列も確認する。

```text
PASS: all five bundled examples completed; sAB error terms match the verified reference.
```

本番サーバーでフォントを再配置する場合は、Ubuntu公式パッケージを一時展開し、次の2ファイルを `/srv/shiny-server/.fonts/` へ置く。

```sh
apt-get download fonts-noto-cjk
dpkg-deb -x fonts-noto-cjk_*_all.deb extracted
cp extracted/usr/share/fonts/opentype/noto/NotoSansCJK-Regular.ttc \
   extracted/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc \
   /srv/shiny-server/.fonts/
chmod -R a+rX /srv/shiny-server/.fonts
```

## 次にやること

- 実利用時の同時実行数が増えた場合は、効果量CIの処理時間とサーバー負荷を確認する。
